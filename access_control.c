#include <stdio.h>
#include <stdbool.h>

bool authorize(const char *user, const char *access_level) {
    // Codice per l'autorizzazione dell'utente
    return true;
}

bool authenticate(const char *username, const char *password) {
    // Codice per l'autenticazione dell'utente
    return true;
}
